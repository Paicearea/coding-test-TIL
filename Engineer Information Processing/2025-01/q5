int Main(int b[], int m, int x) {
    int a = 0;
    while (a < m || b[a] < x) {
        if (b[a] < 0)
            b[a] = -b[a];
        a++;
    }
    return 1;
}


// 1. (    ①    )    2. (    ②    )    3. (    ③    )    4. (    ④    )    5. (    ⑤    )  6. (    ⑥    )
// 문장 커버리지 순서 1 → 2  → (          ⑦           )

// 정답:
// (1) int a = 0
// (2) a < m || b[a] < x
// (3) b[a] < 0
// (4) b[a] = -b[a];
// (5) a++;
// (6) return 1;
// (7) ③ → ④ → ⑤ → ② → ⑥